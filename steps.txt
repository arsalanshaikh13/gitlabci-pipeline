creating basic templates
using templates from common template project
running child pipeline from same project
running child pipeline from another project
setting up github actions trigger to gitlabci
setting up github repo
child pipeline

    - if: "$CI_COMMIT_BRANCH == /^features-.*$/"
    - if: "$CI_PIPELINE_SOURCE == push"
